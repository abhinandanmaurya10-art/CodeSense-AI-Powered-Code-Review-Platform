{
  "name": "codesense-backend",
  "version": "1.0.0",
  "description": "Backend API for the CodeSense AI code review platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch . --ext js,json,env server.js",
    "check": "node --check server.js && node --check middleware/auth.js && node --check models/User.js && node --check models/Review.js && node --check routes/auth.js && node --check routes/review.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.15.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
